<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <form>
      <button class="btn btn-primary" type="submit">Comment on this Blog</button>
    </form>
  </p>
  <p>
    <h2>COMMENTS:</h2>
    <!--{{description}}-->
    <p>*** This is where I want to display a list of existing comments for this blog ***</p>
    <p>{{user.name.id}}</p>
  </p>
  </div>
